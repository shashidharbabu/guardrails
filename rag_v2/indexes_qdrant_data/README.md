# Qdrant Storage

Put the NewRAG v2 local Qdrant storage artifact here.

Expected path:

```text
rag_v2/indexes_qdrant_data/collection/guardrails_rag_v2/storage.sqlite
```

The SQLite storage file is intentionally not committed because it is larger than
GitHub's normal file limit and this repo does not have Git LFS configured.
