# Core ML + inference
vllm>=0.6.0
torch>=2.4.0
transformers>=4.45.0
accelerate>=0.34.0

# LangGraph + LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0

# Langfuse tracing
langfuse>=2.0.0

# OpenAI-compatible client (for vLLM API calls)
openai>=1.40.0

# Vector DB
qdrant-client>=1.10.0

# Retrieval
rank-bm25>=0.2.2
scikit-learn>=1.3.0

# DB + utils
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
numpy>=1.24.0

# Optional: BGE reranker (better per-claim chunk reranking than TF-IDF)
# Already included via transformers
