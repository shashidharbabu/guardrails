# multi_agent — MAD pipeline dependencies
# Install with: pip install -r multi_agent/requirements.txt

# LLM client (Ollama-compatible — uses OpenAI API interface)
openai>=1.30.0

# FastAPI + server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Data validation
pydantic>=2.0.0

# Async HTTP (used by openai client internally)
httpx>=0.27.0

# ── Real RAG (Qdrant + Nemotron-8B) ───────────────────────────────────────────
qdrant-client>=1.9.0
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.30.0
